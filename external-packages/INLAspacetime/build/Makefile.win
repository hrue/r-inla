CFLAGS = $(FLAGS) -mtune=generic -fopenmp -pipe -pthread -fpic -I.
TARGET = libINLAspactime.so
OBJ = cgeneric_ar2ss.o cgeneric_sstspde.o

$(TARGET) : $(OBJ)
	$(CC) -o $@ -shared $^ $(LIBBLAS) -lgfortran -lgomp -lm

clean :; rm -f $(TARGET) $(OBJ)

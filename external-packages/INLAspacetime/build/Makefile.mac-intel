CFLAGS = -framework Accelerate $(FLAGS) -O3 -mtune=generic -fopenmp -pipe -pthread -fpic -I.
LDFLAGS = -framework Accelerate 
TARGET = libINLAspacetime.so
OBJ = cgeneric_ar2ss.o cgeneric_sstspde.o

$(TARGET) : $(OBJ)
	$(CC) $(LDFLAGS) -o $@ -dynamiclib $^ $(LIBBLAS) -lgfortran -lgomp -lm

 clean :; rm -f $(TARGET) $(OBJ)


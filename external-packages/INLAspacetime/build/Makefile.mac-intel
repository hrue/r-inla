CFLAGS = $(FLAGS) -O3 -mtune=generic -fopenmp -pipe -pthread -fpic -I.
TARGET = libINLAspacetime.so
OBJ = cgeneric_ar2ss.o cgeneric_sstspde.o

$(TARGET) : $(OBJ)
	$(CC) -o $@ -dynamiclib $^ $(LIBBLAS) -lgfortran -lgomp -lm

 clean :; rm -f $(TARGET) $(OBJ)


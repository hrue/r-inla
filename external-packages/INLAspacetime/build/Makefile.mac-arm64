ACC = -framework Accelerate
CFLAGS = $(FLAGS) $(ACC) -mtune=native -fopenmp -pipe -pthread -fpic -I.
TARGET = libINLAspactime.so
OBJ = cgeneric_ar2ss.o cgeneric_sstspde.o

$(TARGET) : $(OBJ)
	$(CC) $(ACC) -o $@ -dynamiclib -undefined suppress $^ -lgfortran -lgomp -lm

 clean :; rm -f $(TARGET) $(OBJ)


CFLAGS = $(FLAGS) -O3 -mtune=generic -fopenmp  -fpic $(INC)
CXXFLAGS = $(FLAGS) -O3 -mtune=generic -fopenmp  -fpic $(INC)
OBJC = $(patsubst %.c,%.o,$(wildcard *.c))
OBJCXX = $(patsubst %.cpp,%.o,$(wildcard *.cpp))
all : $(OBJC) $(OBJCXX)

clean :; rm -f $(OBJC) $(OBJCXX)

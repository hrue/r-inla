CXXFLAGS = $(FLAGS) -O2 -mtune=generic -fopenmp -pipe -fpic -I.
LDFLAGS = 
TARGET = libfbesag.so
OBJ = $(patsubst %.c,%.o,$(wildcard *.c))

$(TARGET) : $(OBJ)
	$(CXX) $(LDFLAGS) -o $@ -shared $^ $(LIBBLAS) -lgfortran -lgomp -lm

clean :; rm -f $(TARGET) $(OBJ)

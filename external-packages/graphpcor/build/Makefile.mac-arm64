CFLAGS = -framework Accelerate $(FLAGS) $(INC) -O3 -mtune=native -fopenmp  -fpic -I.
LDFLAGS = -framework Accelerate $(LIB)
TARGET = libgraphpcor.so
OBJ = $(patsubst %.c,%.o,$(wildcard *.c))

$(TARGET) : $(OBJ)
	$(CC) $(LDFLAGS) -o $@ -dynamiclib $^ -lgfortran -lgomp $(LIBLTDL) -lm

 clean :; rm -f $(TARGET) $(OBJ)


#!/bin/bash

case $0 in
    /*)
    DIR=${0%/*};;
    *)
    DIR=$PWD/${0%/*};;
esac
DIR=${DIR%/.}

export DYLD_LIBRARY_PATH=/usr/lib:$DIR:$DYLD_LIBRARY_PATH
export LD_LIBRARY_PATH=/usr/lib:$DIR:$LD_LIBRARY_PATH
exec "$DIR/fmesher" "$@"



% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inla.call.R
\name{inla.binary.path}
\alias{inla.binary.path}
\title{Path of the inla binary in use}
\usage{
inla.binary.path(check = TRUE)
}
\arguments{
\item{check}{Verify the file exists and answers \code{-V}. On failure the reason
is returned as the \code{"error"} attribute rather than thrown, so a
script can test the path without a \code{tryCatch}.}
}
\value{
The path, invisibly when \code{check} fails. \code{NA_character_} when no
binary can be resolved at all.
}
\description{
The absolute path of the \code{inla} program this session would run. That is the
binary an \code{inla()} call uses, and the one to point at a \code{Model.ini} when
running a saved model without R:

\if{html}{\out{<div class="sourceCode">}}\preformatted{<path> -v -t4:1 Model.ini
}\if{html}{\out{</div>}}

A model directory containing \code{Model.ini} comes from
\verb{inla(..., keep = TRUE, working.directory = <dir>)}.
}
\examples{
\dontrun{
inla.binary.path()
system2(inla.binary.path(), c("-v", "-t4:1", "Model.ini"))
}

}
\seealso{
\code{\link[=inla.stiles.install]{inla.stiles.install()}}, \code{\link[=inla.stiles.status]{inla.stiles.status()}}
}

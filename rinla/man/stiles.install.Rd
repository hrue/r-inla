% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stiles.install.R
\name{stiles.install}
\alias{stiles.install}
\alias{inla.stiles.install}
\alias{inla.stiles.status}
\title{Install a pre-built INLA binary with sTiles support}
\usage{
inla.stiles.install(
  tag = NULL,
  dir = NULL,
  force = FALSE,
  smtp = TRUE,
  persist = TRUE,
  repo = "hrue/r-inla",
  verbose = TRUE
)

inla.stiles.status(ping = TRUE, verbose = TRUE)
}
\arguments{
\item{tag}{Release to install from, e.g. \code{"Version_26.08.31"} (tags
published before that use the older \code{"v26.08.18"} form,
and both still resolve). Defaults to the
current release. Pin it for reproducibility.}

\item{dir}{Where to unpack. Defaults to a per-user cache directory.}

\item{force}{Re-download even when the binary is already installed.}

\item{smtp}{Also select the sTiles sparse-matrix backend (default \code{TRUE}).}

\item{persist}{Write the settings into \verb{~/.Rprofile} so they survive an R
restart (default \code{TRUE}). The block is delimited and is
REPLACED on each install, so installing another release
re-points it rather than adding a second entry. A \code{.bak}
is kept the first time the file is modified.}

\item{repo}{GitHub repository holding the releases.}

\item{verbose}{Print the report (default \code{TRUE}).}

\item{ping}{Also run the binary with \code{-ping} to prove it starts (default
\code{TRUE}; costs about a second).}
}
\value{
The path of the installed binary, invisibly.

Invisibly, a list with \code{inla.call}, \code{smtp}, \code{release}, \code{alive},
\code{buildinfo} (character vector) and \code{cache} (installed releases).
}
\description{
\code{inla.stiles.install()} performs what \code{\link[=inla.stiles]{inla.stiles()}} describes: it works
out which pre-built binary matches this machine, downloads it from the
releases page, unpacks it into a cache directory, checks that it runs, and
points \code{inla.call} at it. One call instead of the manual download,
un-pack and \code{inla.setOption()} steps.

The binaries are portable: six of them cover Linux (x86-64 baseline and
x86-64-v3), Linux arm64, macOS (Intel and Apple silicon) and Windows, so the
choice follows from the operating system, the architecture, the C library
version, and the instruction sets the processor actually
supports. Each bundle embeds a matching \code{libstiles}, and carries a
\code{BUILDINFO} file recording the compiler, flags and library versions that
produced it.

\code{inla.stiles.status()} answers the question \code{inla.stiles.install()} leaves
open after a restart: which binary is this session actually pointing at,
with which backend, and what was that binary built from. Everything is read
from the session's options and the binary's own \code{BUILDINFO}, not from what
an earlier install intended.
}
\examples{
\dontrun{
inla.stiles.install()                  # newest release
inla.stiles.install(tag = "Version_26.08.31") # a specific one
}

}
\seealso{
\code{\link[=inla.stiles]{inla.stiles()}}, \code{\link[=inla.binary.install]{inla.binary.install()}}

\code{\link[=inla.stiles.install]{inla.stiles.install()}}
}

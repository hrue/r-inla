% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rw2o1diid.R
\name{inla.rw2o1diid.hyperpar}
\alias{inla.rw2o1diid.hyperpar}
\title{Extract user-scale hyperparameter marginals for an \code{inla.rw2o1diid} term}
\usage{
inla.rw2o1diid.hyperpar(result, name)
}
\arguments{
\item{result}{An \code{"inla"} object returned by \code{\link[=inla]{inla()}}.}

\item{name}{Character. The label of the \code{f()} term that used
\code{inla.rw2o1diid()}, i.e. the first argument to \code{f()}. For
\code{f(time, model = inla.rw2o1diid(n))} this is \code{"time"}.}
}
\value{
A named list with elements \code{tau} and \code{phi}, each an
\code{inla.marginal} object suitable for \code{\link[=inla.zmarginal]{inla.zmarginal()}},
\code{\link[=inla.qmarginal]{inla.qmarginal()}}, etc.
}
\description{
INLA reports \code{inla.rw2o1diid} hyperparameter marginals on the internal
(rgeneric) scale: \code{theta_1 = log(tau)} and \code{theta_2 = logit(phi)}. This
helper transforms them back to the user scale via \code{\link[=inla.tmarginal]{inla.tmarginal()}},
returning marginals for \code{tau} (the total marginal precision) and \code{phi}
(the structured-variance fraction).
}
\examples{
\dontrun{
  r <- inla(
    y ~ f(time, model = inla.rw2o1diid(n)) - 1,
    data = data.frame(y, time)
  )
  hp <- inla.rw2o1diid.hyperpar(r, "time")
  inla.zmarginal(hp$tau)
  inla.zmarginal(hp$phi)
}
}
\seealso{
\code{\link[=inla.rw2o1diid]{inla.rw2o1diid()}}, \code{\link[=inla.tmarginal]{inla.tmarginal()}}, \code{\link[=inla.zmarginal]{inla.zmarginal()}}
}
\author{
Antonio R. Vargas
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inla.stiles.R
\name{inla.stiles}
\alias{inla.stiles}
\alias{inla.stiles.spy.mtx}
\alias{inla.stiles.spy.graph}
\alias{inla.stiles.graph2mtx}
\alias{inla.stiles.mtx2graph}
\title{sTiles helpers: streaming spy plots and graph<->mtx I/O}
\usage{
inla.stiles.spy.mtx(
  file,
  out = NULL,
  GRID = 1000L,
  text = FALSE,
  colorbar = FALSE,
  palette = "grey",
  CHUNK = 2000000L
)

inla.stiles.spy.graph(
  file,
  out = NULL,
  GRID = 1000L,
  text = FALSE,
  colorbar = FALSE,
  palette = "grey",
  CHUNK = 100000L,
  base = NULL
)

inla.stiles.graph2mtx(
  graph_file,
  mtx_file,
  diagonal = c("degree", "one", "none"),
  base = NULL
)

inla.stiles.mtx2graph(mtx_file, graph_file, base = 0L, CHUNK = 2000000L)
}
\arguments{
\item{file}{For \code{inla.stiles.spy.mtx}: path to a \code{.mtx} file.
For \code{inla.stiles.spy.graph}: path to an INLA graph file.}

\item{out}{Output PNG path. Defaults to the input path with
extension replaced by \code{.png}.}

\item{GRID}{Bin grid resolution (default 1000).}

\item{text}{If \code{TRUE}, draw title and axis labels (default \code{FALSE}).}

\item{colorbar}{If \code{TRUE}, draw a colorbar to the right (default \code{FALSE}).}

\item{palette}{Colour palette: \code{"grey"} (default), \code{"gray"}, \code{"binary"},
\code{"viridis"}, or any name accepted by \code{hcl.colors()}.}

\item{CHUNK}{Lines read per streaming chunk. Tune for very large files.}

\item{base}{Index base for INLA graph files. \code{NULL} (default)
auto-detects by peeking at the first data row.}

\item{graph_file}{Path to an INLA graph file (ASCII).}

\item{mtx_file}{Path to a MatrixMarket \code{.mtx} file.}

\item{diagonal}{Diagonal value for \code{inla.stiles.graph2mtx}:
\code{"degree"} (default), \code{"one"}, or \code{"none"}.}
}
\value{
The output file path (invisibly).
}
\description{
Streaming sparsity-pattern plots and conversions between INLA graph
files and MatrixMarket \code{.mtx} files. The spy plots stream the input
file (never loading the full matrix), bin entries into a \verb{GRID x GRID}
grid, and render a log-density image. This makes them usable on
matrices that would otherwise exhaust memory if read into a \code{Matrix}.
}
\details{
INLA graph files are autodetected as 0-based or 1-based by inspecting
the first data row; override with \code{base = 0L} or \code{base = 1L}.
MatrixMarket files use the standard 1-based coordinate convention.

\code{inla.stiles.graph2mtx} writes a symmetric MatrixMarket file. By
default the diagonal is set to the node degree (matches the typical
precision-matrix convention where \code{Q[i,i]} equals the sum of weights of
incident edges, giving a diagonally-dominant Q with unit off-diagonals).
Use \code{diagonal = "one"} for unit diagonals, or \code{diagonal = "none"} to
omit the diagonal entirely.
}
\examples{
\dontrun{
## spy a large MTX file (streamed, never fully loaded)
inla.stiles.spy.mtx("Q.mtx", "Q.png")

## spy an INLA graph file directly
inla.stiles.spy.graph("inla_graph_animal1", "animal.png")

## convert between formats (round-trips bit-identical up to neighbour order)
inla.stiles.graph2mtx("inla_graph_animal1", "animal.mtx")
inla.stiles.mtx2graph("animal.mtx",         "animal.graph")
}
}
\seealso{
\code{\link[=inla.spy]{inla.spy()}}, \code{\link[=inla.read.graph]{inla.read.graph()}}, \code{\link[=inla.write.graph]{inla.write.graph()}}
}
\author{
Esmail Abdul Fattah \email{esmail.abdulfattah@kaust.edu.sa}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rw2o1diid.R
\name{inla.rw2o1diid}
\alias{inla.rw2o1diid}
\alias{inla.rw2o1diid.model}
\title{1D Second-order random walk plus iid noise (BYM2-style parameterisation)}
\usage{
inla.rw2o1diid(
  n,
  prior.tau = list(u = 1, alpha = 0.01),
  prior.phi = list(u = 0.5, alpha = 0.5),
  debug = FALSE
)

inla.rw2o1diid.model(
  cmd = c("graph", "Q", "mu", "initial", "log.norm.const", "log.prior", "quit"),
  theta = NULL
)
}
\arguments{
\item{n}{Integer. Length of the chain. Must be \verb{>= 5} (the minimum size
for which RW2 is well-defined).}

\item{prior.tau}{Named list with entries \code{u} and \code{alpha} for the PC prior
on the precision \code{tau}. Default \code{list(u = 1, alpha = 0.01)}.}

\item{prior.phi}{Named list with entries \code{u} and \code{alpha} for the PC prior
on the mixing parameter \code{phi}. Default \code{list(u = 0.5, alpha = 0.5)},
matching the \code{bym2} default.}

\item{debug}{Logical. Passed to \code{\link[=inla.rgeneric.define]{inla.rgeneric.define()}}.}
}
\value{
An \code{"inla.rgeneric"} object suitable for passing as the \code{model}
argument to \code{\link[=f]{f()}}.
}
\description{
Convenience constructor for a 1D second-order random walk mixed with iid
noise, using the BYM2-style \verb{(tau, phi)} parameterisation. The latent
field has precision
\deqn{Q(\tau, \phi) = \tau \, \bigl( \phi \, R + (1-\phi)\, I \bigr),}
where \code{R} is the RW2 structure matrix scaled to generalised variance one
and \code{I} is the identity. The \code{tau} parameter controls the total marginal
precision and \code{phi} in \verb{(0, 1)} controls the fraction of variance
attributable to the structured RW2 component.
}
\details{
Both hyperparameters use PC priors. The PC prior on \code{tau} is the standard
\code{\link[=inla.pc.dprec]{inla.pc.dprec()}} prior, parameterised by \verb{(u, alpha)} such that
\code{P(1/sqrt(tau) > u) = alpha}. The PC prior on \code{phi} is computed from the
eigenvalues of the scaled RW2 structure matrix using the same KLD-based
derivation as \code{bym2} (see \code{\link[=inla.pc.bym.phi]{inla.pc.bym.phi()}}), parameterised by
\verb{(u, alpha)} such that \code{P(phi < u) = alpha}.
}
\examples{
\dontrun{
  n <- 100
  time <- 1:n
  y <- cumsum(cumsum(rnorm(n, sd = 0.05))) + rnorm(n, sd = 0.5)
  r <- inla(y ~ f(time, model = inla.rw2o1diid(n)) - 1,
            data = data.frame(y, time))
}
}
\seealso{
\code{\link[=f]{f()}}, \code{\link[=inla.rgeneric.define]{inla.rgeneric.define()}}, \code{inla.doc("bym2")}
}
\author{
Antonio Vargas
}

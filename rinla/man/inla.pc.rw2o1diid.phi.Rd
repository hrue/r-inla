% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rw2o1diid.R
\name{inla.pc.rw2o1diid.phi}
\alias{inla.pc.rw2o1diid.phi}
\title{PC prior on phi for the rw2o1diid (precision-mixture) model}
\usage{
inla.pc.rw2o1diid.phi(eigenvalues, u, alpha, return.as.table = FALSE)
}
\arguments{
\item{eigenvalues}{Numeric vector of eigenvalues of the (scaled) RW2
structure matrix \code{R}, including any zeros from the null space.}

\item{u, alpha}{Calibration parameters for the PC prior on \code{phi}, such
that \code{P(phi < u) = alpha}. Both must lie strictly in \verb{(0, 1)}.}

\item{return.as.table}{Currently unused; retained for parity with
\code{\link[=inla.pc.bym.phi]{inla.pc.bym.phi()}}. The function always returns a closure.}
}
\value{
A function of one argument \code{phi} returning the log-density of the
PC prior on \code{phi}, evaluated on \verb{(0, 1)}.
}
\description{
Builds the penalised-complexity prior on the mixing parameter \code{phi} of an
\code{inla.rw2o1diid} term. The flexible model has covariance
\deqn{\Sigma(\phi) = \tau^{-1}\bigl(\phi R + (1-\phi) I\bigr)^{-1}}
and the base model is \code{phi = 0} (pure iid with covariance \verb{tau^\{-1\} I}).
With \code{tau} held fixed, the Gaussian KLD between flexible and base reduces
to
\deqn{2\,\mathrm{KLD}(\phi) = \sum_i \frac{1}{\phi\lambda_i + (1-\phi)} - n + \sum_i \log\bigl(\phi\lambda_i + (1-\phi)\bigr),}
where the \code{lambda_i} are the eigenvalues of \code{R} (including any zeros from
the null space). The PC distance is \code{dist(phi) = sqrt(2*KLD(phi))}, and
the prior on distance is \code{Exp(lambda)} with \code{lambda} chosen so that
\code{P(phi < u) = alpha}.
}
\details{
This is the precision-mixture analogue of \code{\link[=inla.pc.bym.phi]{inla.pc.bym.phi()}}, which
derives the PC prior from BYM2's \emph{variance-mixture} KLD. The two priors
agree at \code{phi = 0} and approximately at \code{phi = 1}, but differ in the
interior.
}
\seealso{
\code{\link[=inla.rw2o1diid]{inla.rw2o1diid()}}, \code{\link[=inla.pc.bym.phi]{inla.pc.bym.phi()}}
}
\author{
Antonio R. Vargas
}

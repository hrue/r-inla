		if (1){
			size_t ii;
			for(ii=0; ii<d->log_correction->size; ii++){
				printf("i %d xx %g yy %g \n", ii, d->log_correction->x[ii], d->log_correction->y[ii]);
			}

			exit(0);
		}


		printf("%s\n", d->log_correction->interp->type->name);
		exit(0);
